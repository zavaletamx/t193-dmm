import React from 'react';
import { Text, View } from 'react-native';

const ListaDeseos = (props) => {
	return (
		<View>
			<Text>ListaDeseos.js</Text>
		</View>
	);
};

export default ListaDeseos;
